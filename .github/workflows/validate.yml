name: Validate maps
on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate every map against the schema and check index.json hashes
        run: python3 tools/update_index.py --check
